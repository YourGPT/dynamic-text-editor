{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "installCommand": "npm run use-published && npm install",
        "buildCommand": "npm run build"
      }
    }
  ]
}
