{
  "buildCommand": "npm run build:demo",
  "outputDirectory": "demo/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "installCommand": "npm install --ignore-scripts && cd demo && npm install"
}
